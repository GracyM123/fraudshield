# Simple Dockerfile - expects pre-built JAR in target/
FROM eclipse-temurin:17-jre-alpine
WORKDIR /app
COPY target/fraud-detection-backend-1.0.0.jar app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-XX:+UseContainerSupport", "-XX:MaxRAMPercentage=75.0", "-jar", "app.jar"]
